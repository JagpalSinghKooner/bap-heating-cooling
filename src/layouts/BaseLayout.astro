---
import '../styles/global.css';
import { getBusinessProfile, getCompanyName, getPhoneDisplay, getPhoneLink, getEmail, getEmailLink } from '../lib/getBusinessProfile';
import { getCanonicalUrl } from '../lib/urlGovernance';
import logo from '../images/bapheatingandcoolingltd.webp';
import SeasonalCalloutBar from '../components/header/SeasonalCalloutBar.astro';
import Header from '../components/header/Header.astro';
import FloatingCTA from '../components/primitives/FloatingCTA.astro';
import ExitIntentModal from '../components/shared/ExitIntentModal.astro';

export interface Props {
  title?: string;
  description?: string;
  canonicalURL?: string;
  robots?: string;
  schema?: object | object[];
}

const profile = await getBusinessProfile();
const companyName = getCompanyName(profile);

const {
  title = companyName,
  description = 'Professional heating and cooling services',
  canonicalURL,
  robots = 'index,follow',
  schema,
} = Astro.props;

// Get canonical URL using governance helper
const canonicalUrl = canonicalURL || getCanonicalUrl(Astro.url.pathname);

// Environment-based indexing control
const environment = import.meta.env.PUBLIC_ENVIRONMENT || 'production';
const isProduction = environment === 'production';

// Override robots for staging
const effectiveRobots = isProduction ? robots : 'noindex,nofollow';

// GA4 Configuration
const ga4Id = import.meta.env.PUBLIC_GA4_ID;
const enableGA4 = import.meta.env.PROD && ga4Id && isProduction;
---

<!doctype html>
<html lang="en-CA">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <meta name="robots" content={effectiveRobots} />
    <link rel="canonical" href={canonicalUrl} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- SEO -->
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <!-- Structured Data (JSON-LD) -->
    {schema && (
      <script type="application/ld+json" set:html={JSON.stringify(
        Array.isArray(schema) ? schema : [schema]
      )} />
    )}

    <!-- Google Analytics 4 -->
    {enableGA4 && (
      <>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${ga4Id}`}></script>
        <script is:inline define:vars={{ ga4Id }}>
          // Initialize Google Analytics 4 with consent management
          const CONSENT_KEY = 'bap_analytics_consent';

          function getAnalyticsConsent() {
            try {
              const stored = localStorage.getItem(CONSENT_KEY);
              if (stored === 'granted' || stored === 'denied') {
                return stored;
              }
            } catch (e) {
              console.warn('Failed to read analytics consent:', e);
            }
            return 'granted'; // Default: granted
          }

          const consent = getAnalyticsConsent();

          window.dataLayer = window.dataLayer || [];
          function gtag() { dataLayer.push(arguments); }

          gtag('consent', 'default', {
            'analytics_storage': consent,
            'ad_storage': consent,
            'wait_for_update': 500
          });

          gtag('js', new Date());
          gtag('config', ga4Id, {
            'send_page_view': true,
            'anonymize_ip': true
          });

          console.info('GA4 initialized:', ga4Id, 'consent:', consent);
        </script>
      </>
    )}
  </head>
  <body>
    <!-- Skip to main content link for accessibility -->
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-0 focus:left-0 focus:z-50 focus:p-4 focus:bg-primary focus:text-primary-foreground">
      Skip to main content
    </a>

    <div class="flex min-h-screen flex-col">
      <!-- Seasonal Callout Bar (sticky only) -->
      <SeasonalCalloutBar />

      <!-- Header (non-sticky) -->
      <header role="banner">
        <Header />
      </header>

      <!-- Main Content -->
      <main id="main-content" role="main" class="flex-1">
        <slot />
      </main>

      <!-- Footer -->
      <footer class="border-t bg-muted" role="contentinfo">
        <div class="mx-auto max-w-6xl px-4 py-8 sm:px-6 sm:py-12 lg:px-8">
          <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
            <!-- Company Info -->
            <div class="sm:col-span-2 lg:col-span-1">
              <img
                src={logo.src}
                alt={companyName}
                class="mb-4 h-8 w-auto"
              />
              <p class="text-sm text-muted-foreground">
                Professional heating and cooling services for residential and commercial properties.
              </p>
              <div class="mt-4 space-y-1 text-sm text-muted-foreground">
                <p>Established {profile.business.established_year}</p>
                <p>{profile.warranty.statement}</p>
              </div>
            </div>

            <!-- Quick Links -->
            <div>
              <h4 class="mb-4 text-sm font-semibold">Quick Links</h4>
              <ul class="space-y-2 text-sm">
                <li>
                  <a href="/services/" class="text-muted-foreground transition-colors hover:text-foreground">
                    Services
                  </a>
                </li>
                <li>
                  <a href="/locations/" class="text-muted-foreground transition-colors hover:text-foreground">
                    Locations
                  </a>
                </li>
                <li>
                  <a href="/about-us/" class="text-muted-foreground transition-colors hover:text-foreground">
                    About Us
                  </a>
                </li>
                <li>
                  <a href="/contact-us/" class="text-muted-foreground transition-colors hover:text-foreground">
                    Contact Us
                  </a>
                </li>
              </ul>
            </div>

            <!-- Contact -->
            <div>
              <h4 class="mb-4 text-sm font-semibold">Contact</h4>
              <ul class="space-y-3 text-sm">
                <li>
                  <a href={getPhoneLink(profile)} class="flex items-center gap-2 text-muted-foreground transition-colors hover:text-foreground">
                    <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                    </svg>
                    <span>{getPhoneDisplay(profile)}</span>
                  </a>
                </li>
                <li>
                  <a href={getEmailLink(profile)} class="flex items-center gap-2 text-muted-foreground transition-colors hover:text-foreground">
                    <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                    <span class="break-all">{getEmail(profile)}</span>
                  </a>
                </li>
                {profile.contact.booking.enabled && (
                  <li class="pt-2">
                    <a
                      href={profile.contact.booking.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="inline-block rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
                    >
                      Book Online
                    </a>
                  </li>
                )}
              </ul>
            </div>

            <!-- Social & Reviews -->
            <div>
              <h4 class="mb-4 text-sm font-semibold">Connect</h4>
              <ul class="space-y-2 text-sm">
                {profile.social.facebook && (
                  <li>
                    <a
                      href={profile.social.facebook}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="flex items-center gap-2 text-muted-foreground transition-colors hover:text-foreground"
                    >
                      <svg class="h-4 w-4 flex-shrink-0" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                      </svg>
                      <span>Facebook</span>
                    </a>
                  </li>
                )}
                {profile.social.instagram && (
                  <li>
                    <a
                      href={profile.social.instagram}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="flex items-center gap-2 text-muted-foreground transition-colors hover:text-foreground"
                    >
                      <svg class="h-4 w-4 flex-shrink-0" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                        <path d="M12.315 2c2.43 0 2.784.013 3.808.06 1.064.049 1.791.218 2.427.465a4.902 4.902 0 011.772 1.153 4.902 4.902 0 011.153 1.772c.247.636.416 1.363.465 2.427.048 1.067.06 1.407.06 4.123v.08c0 2.643-.012 2.987-.06 4.043-.049 1.064-.218 1.791-.465 2.427a4.902 4.902 0 01-1.153 1.772 4.902 4.902 0 01-1.772 1.153c-.636.247-1.363.416-2.427.465-1.067.048-1.407.06-4.123.06h-.08c-2.643 0-2.987-.012-4.043-.06-1.064-.049-1.791-.218-2.427-.465a4.902 4.902 0 01-1.772-1.153 4.902 4.902 0 01-1.153-1.772c-.247-.636-.416-1.363-.465-2.427-.047-1.024-.06-1.379-.06-3.808v-.63c0-2.43.013-2.784.06-3.808.049-1.064.218-1.791.465-2.427a4.902 4.902 0 011.153-1.772A4.902 4.902 0 015.45 2.525c.636-.247 1.363-.416 2.427-.465C8.901 2.013 9.256 2 11.685 2h.63zm-.081 1.802h-.468c-2.456 0-2.784.011-3.807.058-.975.045-1.504.207-1.857.344-.467.182-.8.398-1.15.748-.35.35-.566.683-.748 1.15-.137.353-.3.882-.344 1.857-.047 1.023-.058 1.351-.058 3.807v.468c0 2.456.011 2.784.058 3.807.045.975.207 1.504.344 1.857.182.466.399.8.748 1.15.35.35.683.566 1.15.748.353.137.882.3 1.857.344 1.054.048 1.37.058 4.041.058h.08c2.597 0 2.917-.01 3.96-.058.976-.045 1.505-.207 1.858-.344.466-.182.8-.398 1.15-.748.35-.35.566-.683.748-1.15.137-.353.3-.882.344-1.857.048-1.055.058-1.37.058-4.041v-.08c0-2.597-.01-2.917-.058-3.96-.045-.976-.207-1.505-.344-1.858a3.097 3.097 0 00-.748-1.15 3.098 3.098 0 00-1.15-.748c-.353-.137-.882-.3-1.857-.344-1.023-.047-1.351-.058-3.807-.058zM12 6.865a5.135 5.135 0 110 10.27 5.135 5.135 0 010-10.27zm0 1.802a3.333 3.333 0 100 6.666 3.333 3.333 0 000-6.666zm5.338-3.205a1.2 1.2 0 110 2.4 1.2 1.2 0 010-2.4z"/>
                      </svg>
                      <span>Instagram</span>
                    </a>
                  </li>
                )}
                {profile.reputation.google_review_count > 0 && (
                  <li class="pt-2">
                    <a
                      href="https://www.google.com/search?q=B.A.P+Heating+%26+Cooling"
                      target="_blank"
                      rel="noopener noreferrer"
                      class="inline-flex items-center gap-2 text-sm text-muted-foreground transition-colors hover:text-foreground"
                    >
                      <svg viewBox="0 0 24 24" class="h-4 w-4 flex-shrink-0" aria-hidden="true">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                      </svg>
                      <span>{profile.reputation.google_rating} ({profile.reputation.google_review_count}+ reviews)</span>
                    </a>
                  </li>
                )}
              </ul>
            </div>
          </div>

          <!-- Footer Bottom -->
          <div class="mt-8 border-t pt-6 sm:pt-8">
            <div class="flex flex-col items-center justify-between gap-3 text-center text-sm text-muted-foreground sm:gap-4 md:flex-row md:text-left">
              <p>&copy; {new Date().getFullYear()} {profile.business.legal_name}. All rights reserved.</p>
              <div class="flex flex-wrap items-center justify-center gap-2 sm:gap-4">
                <span>{profile.locations.primary.city}, {profile.locations.primary.province}</span>
                <span class="hidden sm:inline">|</span>
                <span>{profile.locations.secondary.city}, {profile.locations.secondary.province}</span>
              </div>
            </div>
          </div>
        </div>
      </footer>

      <!-- Floating Mobile CTA -->
      <FloatingCTA variant="call" />

      <!-- Exit Intent Modal -->
      <ExitIntentModal />
    </div>

    <!-- Analytics Event Tracking -->
    <script>
      // Auto-track phone clicks, booking clicks, email clicks, and outbound links
      document.addEventListener('DOMContentLoaded', () => {
        // Track phone link clicks (including emergency detection)
        document.querySelectorAll('a[href^="tel:"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const target = e.currentTarget as HTMLAnchorElement;
            if (target) {
              const isEmergency = target.getAttribute('data-cta') === 'emergency';
              const context = target.getAttribute('data-cta-context') || 'unknown';

              if (typeof window.gtag !== 'undefined') {
                if (isEmergency) {
                  // Track as emergency call
                  window.gtag('event', 'emergency_call_click', {
                    event_category: 'engagement',
                    event_label: context,
                    value: 1
                  });
                } else {
                  // Track as regular call
                  window.gtag('event', 'phone_call_click', {
                    event_category: 'engagement',
                    event_label: context,
                    value: 1
                  });
                }
              }
            }
          });
        });

        // Track email link clicks
        document.querySelectorAll('a[href^="mailto:"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const target = e.currentTarget as HTMLAnchorElement;
            if (target) {
              const context = target.getAttribute('data-cta-context') || 'unknown';

              if (typeof window.gtag !== 'undefined') {
                window.gtag('event', 'email_click', {
                  event_category: 'engagement',
                  event_label: context,
                  value: 1
                });
              }
            }
          });
        });

        // Track booking button clicks
        document.querySelectorAll('a[href*="housecallpro.com"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const target = e.currentTarget as HTMLAnchorElement;
            if (target) {
              const context = target.getAttribute('data-cta-context') ||
                             target.closest('[data-source]')?.getAttribute('data-source') ||
                             'unknown';
              if (typeof window.gtag !== 'undefined') {
                window.gtag('event', 'booking_click', {
                  event_category: 'conversion',
                  event_label: context,
                  value: 1
                });
              }
            }
          });
        });

        // Track outbound links
        document.querySelectorAll('a[target="_blank"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const target = e.currentTarget as HTMLAnchorElement;
            if (target && target.hostname !== window.location.hostname && typeof window.gtag !== 'undefined') {
              window.gtag('event', 'outbound_link', {
                event_category: 'engagement',
                event_label: target.href
              });
            }
          });
        });

        // Track service area region link clicks
        document.querySelectorAll('a[data-cta-context="service-areas-region"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const target = e.currentTarget as HTMLAnchorElement;
            if (target && typeof window.gtag !== 'undefined') {
              const regionName = target.getAttribute('data-region-name') || 'unknown';
              window.gtag('event', 'service_area_region_click', {
                event_category: 'navigation',
                event_label: regionName,
                value: 1
              });
            }
          });
        });

        // Track service area city link clicks
        document.querySelectorAll('a[data-cta-context="service-areas-city"]').forEach((link) => {
          link.addEventListener('click', (e) => {
            const target = e.currentTarget as HTMLAnchorElement;
            if (target && typeof window.gtag !== 'undefined') {
              const cityName = target.getAttribute('data-city-name') || 'unknown';
              const regionName = target.getAttribute('data-region-name') || 'unknown';
              window.gtag('event', 'service_area_city_click', {
                event_category: 'navigation',
                event_label: `${regionName} - ${cityName}`,
                value: 1
              });
            }
          });
        });
      });
    </script>
  </body>
</html>
